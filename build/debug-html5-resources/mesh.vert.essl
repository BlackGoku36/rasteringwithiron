#version 100

uniform mat3 N;
uniform mat4 WVP;

varying vec3 wnormal;
attribute vec3 nor;
attribute vec4 pos;

void main()
{
    wnormal = normalize(N * nor);
    gl_Position = WVP * vec4(pos.xyz, 1.0);
}

